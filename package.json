{
  "name": "pathfinding",
  "version": "1.0.0",
  "description": "Hopefully a simple pathfinding demo.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle:uglify:watch": "globify -w -v -d \"src/**/*.js\" -o static/bundle.js",
    "bundle:build": "browserify --debug src/main.js > static/bundle.js"
  },
  "author": "Etienne Buschong",
  "license": "ISC",
  "devDependencies": {
    "globify": "^2.3.8",
    "uglifyify": "^5.0.2",
    "watchify": "^3.11.1"
  }
}
